{
  "name": "property-flow",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@property-flow/client",
    "build": "npm run build --workspace=@property-flow/client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
